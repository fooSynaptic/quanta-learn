# Example SDPO smoke config sketch (ch12).
# Placeholder only — load with upstream distillation.train.

model_name: Qwen/Qwen3-1.7B
loss: sdpo
data:
  size: 2000
  specs:
    - name: spell_backward
      weight: 1
      config:
        min_word_len: 3
        max_word_len: 10
kl_top_k: 20
success_reward_threshold: 1.0
temperature: 0.6
top_p: 0.95
top_k: 20
min_p: 0.0
repetition_penalty: 1.0
max_new_tokens: 512
max_prompt_len: 512
max_reprompt_len: 1024
enable_thinking: true
lr: 1.0e-6
warmup_ratio: 0.0
prompts_per_step: 16
num_rollouts: 8
num_steps: 40
max_norm: 1.0
seed: 42
wandb_entity: null
wandb_project: null
wandb_run_name: sdpo_smoke
